<?php

/**
 * Sets whether we set players directly
 * from the cli on runtime.
 */
define('TP_INPUT', 'automatic');

/**
 * Set to 'true' if you want to see only
 * the message from an exception error.
 */
define('TP_CLEAN_DEBUG', true);

/**
 * Below configuration settings only works
 * with automatic input.
 */
define('TP_PLAYERS', ['A', 'B', 'C', 'D', 'E']);
